/**
 * @cerebrum/shared-utils — public entry point.
 *
 * Small, generic, dependency-free utility functions only — no domain or
 * feature awareness, no side effects, no framework coupling (mirrors the
 * backend's apps/backend/src/cerebrum/utils/ package's same discipline).
 * If a "shared" utility starts encoding business rules, it belongs in a
 * domain-specific location, not here.
 *
 * Empty at Repository Foundation.
 */

export {};
