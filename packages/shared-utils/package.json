{
  "name": "@cerebrum/shared-utils",
  "version": "0.1.0",
  "private": true,
  "description": "Small, generic, dependency-free TypeScript utility functions shared across TypeScript packages/apps.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist node_modules/.cache"
  },
  "devDependencies": {
    "@cerebrum/eslint-config": "workspace:*",
    "@cerebrum/tsconfig": "workspace:*",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
