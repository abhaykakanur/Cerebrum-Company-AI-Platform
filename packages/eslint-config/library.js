/**
 * Cerebrum ESLint configuration for plain TypeScript library packages
 * (e.g. packages/shared-types, packages/shared-utils) — no framework-
 * specific rules layered on top of the shared base.
 */
module.exports = {
  extends: ["./index.js"],
};
