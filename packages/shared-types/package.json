{
  "name": "@cerebrum/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript types shared between the frontend and any other TypeScript consumer (e.g., API response shapes) — the frontend-side mirror of the backend's DTOs. See docs/architecture/specification/34_Architecture_Principles.md's DTO pattern.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist node_modules/.cache"
  },
  "devDependencies": {
    "@cerebrum/eslint-config": "workspace:*",
    "@cerebrum/tsconfig": "workspace:*",
    "typescript": "^5.6.0"
  }
}
