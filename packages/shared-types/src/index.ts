/**
 * @cerebrum/shared-types — public entry point.
 *
 * Empty at Repository Foundation, deliberately. This package exists as
 * the established location for TypeScript types shared between the
 * frontend and other future TypeScript consumers, mirroring backend DTOs
 * (docs/architecture/specification/34_Architecture_Principles.md) once
 * an API contract exists to describe (Phase 2 onward, see
 * docs/architecture/specification/110_Implementation_Roadmap.md).
 *
 * Populate this file (or split it into src/<domain>.ts modules re-exported
 * here) as each API surface is implemented — never define a shared type
 * ad hoc inside a frontend component.
 */

export {};
