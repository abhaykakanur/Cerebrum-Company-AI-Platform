{
  "name": "@cerebrum/shared-config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared, non-secret runtime configuration constants for TypeScript consumers (e.g., feature flag keys, well-known routes). See docs/architecture/specification/37_Configuration_Strategy.md.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist node_modules/.cache"
  },
  "devDependencies": {
    "@cerebrum/eslint-config": "workspace:*",
    "@cerebrum/tsconfig": "workspace:*",
    "typescript": "^5.6.0"
  }
}
