# @cerebrum/shared-config

Shared, non-secret runtime configuration constants for TypeScript
consumers.

## Public Interfaces

`src/index.ts` — currently empty.

## Dependencies

None.

## Limitations

**Never put a secret here.** This package is version-controlled and
bundled into the frontend — see
`docs/architecture/specification/75_Security_Architecture.md`. Empty at
Repository Foundation.
