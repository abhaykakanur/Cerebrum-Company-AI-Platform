# types/

Frontend-local TypeScript types not shared with the backend or other
packages. Cross-cutting shared types belong in `packages/shared-types`
instead — see `docs/architecture/naming-conventions.md`. Empty at
Repository Foundation.
