# utils/

Small, generic, dependency-free TypeScript utility functions with no
feature or domain awareness — the frontend counterpart to the backend's
`apps/backend/src/cerebrum/utils/`. Empty at Repository Foundation.
