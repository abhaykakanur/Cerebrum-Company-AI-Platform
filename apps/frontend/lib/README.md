# lib/

The typed API client consuming the backend's Public/Administrative API
surface (`docs/architecture/specification/80_API_Architecture.md`) and
other integration-adjacent library code. Empty at Repository Foundation —
no API endpoints exist yet for a client to call.
