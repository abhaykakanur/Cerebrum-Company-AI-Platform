# components/

Design System components — the reusable Buttons, Inputs, Cards, Dialogs,
etc. catalogued in `docs/architecture/specification/87_Component_Library.md`.

Per the Design-System-First mandate
(`docs/architecture/specification/85_Frontend_Architecture.md`), every
page and feature is built exclusively from what lives here. Empty at
Repository Foundation — populated starting with the Design System
implementation milestone.
