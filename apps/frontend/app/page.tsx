// Placeholder root route proving the build/serve pipeline works end to end.
// This is scaffolding only — no dashboard, no business content. See
// docs/architecture/specification/110_Implementation_Roadmap.md for when
// real pages (Phase 8+, Enterprise Chat / Dashboard) are introduced.

export default function Home() {
  return (
    <main>
      <p>Cerebrum — repository foundation. No application pages exist yet.</p>
    </main>
  );
}
