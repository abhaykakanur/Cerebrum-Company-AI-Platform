# hooks/

Shared React hooks with no business/feature awareness (e.g., a generic
`useDebounce`, `useMediaQuery`). Feature-specific hooks live alongside
their feature in `features/`, not here. Empty at Repository Foundation.
