"""Cross-domain application services that compose multiple domains'
application-layer use cases (e.g., the Administration Domain's composed
workspace/user/connector administration, per
docs/architecture/specification/35_Domain_Architecture.md's Administration
Domain entry, which explicitly owns no primary state of its own).
"""
