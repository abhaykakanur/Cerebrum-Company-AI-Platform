"""Concrete PostgreSQL repositories satisfying
:class:`~cerebrum.repositories.base.AbstractRepository` for the Identity
& Security platform's models (CIS Phase 1 Prompt 5) — the first
consumers of the Repository Foundation built in Phase 1 Prompt 4.

No business repository exists here — see this package's individual
modules, each scoped to exactly one of the foundational models in
cerebrum.infrastructure.database.models.
"""
