# Integration Tests (Backend)

Tests against real (typically containerized) infrastructure — database,
search, graph, vector store, connectors, queues, workers, authentication.
See `docs/architecture/specification/98_Testing_Strategy.md`'s
Integration Testing section. Requires `scripts/start.sh` to have been run
first.
