# End-to-End Tests (Backend)

Full user-journey tests exercising the complete backend request/response
cycle. See `docs/architecture/specification/98_Testing_Strategy.md`'s
End-to-End Testing section. Full-stack (frontend + backend) E2E tests
live in the root `tests/e2e/` instead — see that directory's README for
the distinction.
