# ==============================================================================
# Cerebrum CI Pipeline — PLACEHOLDER
#
# This is a placeholder, not a real CI pipeline. It is deliberately
# workflow_dispatch-only (manually triggered) so it never runs
# automatically on push/PR — no CI implementation exists yet, per this
# milestone's explicit scope.
#
# The full pipeline, once implemented, follows the thirteen stages in
# docs/architecture/specification/97_CICD_Architecture.md:
#   1. Static Analysis        8. Security Scanning
#   2. Formatting              9. Build Verification
#   3. Linting                 10. Artifact Generation
#   4. Type Checking            11. Deployment Approval
#   5. Dependency Validation     12. Deployment
#   6. Unit Tests                  13. Post-Deployment Validation
#   7. Integration Tests
#
# This placeholder implements a "hello world" verification job only — it
# proves the Actions pipeline mechanism is wired up, nothing more.
# ==============================================================================

name: CI (placeholder)

on:
  workflow_dispatch: {}

jobs:
  placeholder:
    name: Placeholder verification
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Confirm repository structure
        run: |
          echo "This is a placeholder CI workflow."
          echo "See docs/architecture/specification/97_CICD_Architecture.md for the pipeline this will become."
          test -f pyproject.toml
          test -f package.json
          test -f infrastructure/docker/docker-compose.yml
          echo "Repository foundation structure confirmed present."
