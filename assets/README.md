# assets/

Reserved for shared static assets not specific to the frontend app (e.g.,
brand assets used in both product and documentation). Empty at Repository
Foundation — see `apps/frontend/public/` for frontend-served static
assets instead.
